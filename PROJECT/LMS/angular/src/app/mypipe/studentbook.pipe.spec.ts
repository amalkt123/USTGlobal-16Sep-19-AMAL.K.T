import { StudentbookPipe } from './studentbook.pipe';

describe('StudentbookPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentbookPipe();
    expect(pipe).toBeTruthy();
  });
});
