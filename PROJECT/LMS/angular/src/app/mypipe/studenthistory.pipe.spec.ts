import { StudenthistoryPipe } from './studenthistory.pipe';

describe('StudenthistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new StudenthistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
