import { SearchlibrarianPipe } from './searchlibrarian.pipe';

describe('SearchlibrarianPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchlibrarianPipe();
    expect(pipe).toBeTruthy();
  });
});
